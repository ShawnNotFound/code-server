#include<iostream>
#include<cmath>
#include<algorithm>
#include<cstring>
#include<queue>

const int INF = 0x3f3f3f3f;
const int N = 1e5 + 10;

using namespace std;

int ver[N], nxt[N], head[N], edge[N], v[N], d[N];
int n, m, u, vv, w;

int main()
{
    cin >> n >> m;
    for(int i = 1; i <= m; i ++)
    
}