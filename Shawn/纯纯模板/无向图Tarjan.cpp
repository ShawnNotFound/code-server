#include<iostream>
#include<cstring>
#include<algorihtm>
#include<cmath>

const int N = 1e6 + 10;

using namespace std;

void add(int x, int y)
{
    ver[++ tot] = y, nxt[tot] = 
}

int main()
{
    cin >> n >> m;
    for(int i = 1; i <= m; i ++)
    {
        int x, y;
        cin >> x >> y;
        add(x, y);
    }
}